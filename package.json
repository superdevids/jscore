{
  "name": "speexjs-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "SpeexJS Monorepo — Fullstack TypeScript framework (Server, Client, Schema, RPC, CLI, Database, Auth, Cache, Storage)",
  "scripts": {
    "build:speexjs": "cd packages/speexjs && npm run build",
    "build:all": "npm run build:speexjs",
    "publish:speexjs": "cd packages/speexjs && npm publish"
  }
}
